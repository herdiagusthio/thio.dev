# Journal — September 2026

## 2026-09-06
- **Public:** true
- **Location:** Bogor / Bintaro
- **Session Summary:** Connected to IDCloudHost VPS (`HermesHost`, `103.217.144.64`) via SSH (`ssh hermes`). Integrated Garry Tan's **GBrain** (v0.48.2.0) and **GStack** (54 skills) into **Hermes Agent** (`hermes-gateway.service`). Configured local PostgreSQL 16 with `pgvector` as the backing engine, initialized GBrain using the `--no-embedding` workaround (native PostgreSQL `tsvector` + BM25 keyword search) to operate without external embedding API keys, federated `second-brain` (`/home/thiohermes/code-storage/second-brain`) as the default source (indexed 98 markdown files and 148 chunks), compiled and installed 54 Hermes-native GStack skills, and wired GBrain as a stdio MCP server in `~/.hermes/config.yaml`.
- **Key Decisions:**
  1. Backed GBrain with the existing local PostgreSQL 16 instance rather than PGLite for stability and native `pgvector` support.
  2. Applied `--no-embedding` mode during `gbrain init` to allow full functionality via PostgreSQL BM25/`tsvector` without dependency on Voyage AI or OpenAI keys.
  3. Granted `SUPERUSER` / `BYPASSRLS` privileges to the `gbrain` PostgreSQL role to enable schema migrations (v24 and v35 event triggers).
  4. Used GStack's native Hermes compilation (`bun run gen:skill-docs --host hermes`) and installed the skills into `~/.hermes/skills/`.
  5. Registered GBrain as an MCP server (`gbrain serve`) under `mcp_servers` in `~/.hermes/config.yaml`, verified tool discovery (`recall`, `context_pack`, `code_def`, `open_loops`, etc.), and verified `hermes-gateway` service reload.
- **Next Session Focus:** Test Hermes Agent interactions using GStack skills (`/plan-ceo-review`, `/plan-eng-review`, `/review`) and verify autonomous memory recalls from `second-brain`.

<!-- Future days below -->
